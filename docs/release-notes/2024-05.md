# Release Notes – May 2024

## Added
- Redis-backed caching for `/api/candles` and `/api/symbols`.
- AI safety pipeline with prompt/response filters.
- Razorpay integration skeleton and nightly invoicing task.

## Fixed
- API response schemas now validated via Pydantic models.
- Added security headers and HTTPS enforcement middleware.

## Known Issues
- Payments module still uses dummy Razorpay order IDs in local dev.
- AI retriever currently uses placeholder data until pgvector rollout.

## Testing Summary
- Backend coverage: 82%
- Frontend unit tests + Cypress smoke run (Dashboard, Landing).
- Load test: 2k rpm @ 110ms P95 on staging.
