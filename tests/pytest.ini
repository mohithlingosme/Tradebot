[pytest]
# Local overrides for developers that execute pytest from tests/ directly.
minversion = 7.0
addopts = -q --capture=sys
testpaths = .
markers =
    asyncio: marks async tests that require an event loop
