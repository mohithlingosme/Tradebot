# Technical indicators layer (optional) builds on trading stack
-r requirements-trading.txt

# `ta` (pandas-ta) provides a solid indicator set while keeping numpy<2 compatibility.
# Downgraded to 0.10.2 for Python 3.10 compatibility (0.11.0 requires Python 3.11+)
ta~=0.10.2

# TA-Lib is optional. It requires system-level TA-Lib libraries and C/C++ build tools.
# On Windows, prefer pandas-ta (ta) or install via WSL2/Docker if you need TA-Lib.
# ta-lib~=0.4.28
